package weatherproject;

/**
 * Reads the weather info from weather API.
 */
public class WeatherReader {
    private String cityName;
    public WeatherReader(String cityName) {
        this.cityName = cityName;
    }
}
